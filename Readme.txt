Colour Palette
--------------

Reads in palette files which each contain a list of hex colour values, and tiles them onto the display. When a tile is moused over, it will enlarge the tile for easier viewing. Written in VB.NET.